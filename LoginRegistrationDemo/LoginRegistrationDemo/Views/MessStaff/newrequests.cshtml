using LoginRegistrationDemo.Models;
@model IEnumerable<LoginRegistrationDemo.Models.User>


@{
    ViewBag.Title = "newrequests";
    Layout = "~/Views/Shared/_MessStaff.cshtml";
}

<h2>newrequests</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<body>
    <div class="card-body table-full-width table-responsive">
        <table class="table table-hover table-striped">
            <thead>
            <th>UserID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Registrataion Number</th>
            <th>Phone Number</th>
            <th>CNIC</th>



            <th>Hostel Name</th>
            <th>Room Number</th>
            <th>Degree</th>
            <th>Department</th>
            <th>Approve</th>


            </thead>


          
           @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Username)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.RegistrationNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PhoneNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CNIC)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.HostelName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.RoomNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Degree)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Department)
                </td>
                <td class="col-md-10 text-center">
                    @using (Html.BeginForm( "Practice", "MessStaff", FormMethod.Post, new { @class = "form-inline" }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.HiddenFor(m => item.ID);
                        <button type="submit" class="btn btn-xs">Accept</button>
                        }
                        </td>

                        @*<td>
                        @Html.DisplayFor(modelItem => item.Password)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.IsValid)
                    </td>*@
                        <td>
                        @*@Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
                        @Html.ActionLink("Details", "Details", new { id = item.ID }) |*@
                        @Html.ActionLink("practice", "practice", new { id = item.ID })
                    </td>
                        </tr>
                        }

                    </table>
                    </body>
                    <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
                    <script type="text/javascript" src="http://ajax.cdnjs.com/ajax/libs/json2/20110223/json2.js"></script>
                    <script type="text/javascript">
